# 1. DATABASE CONNECTION (Direct/Session via Port 5432)
spring.datasource.url=jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:5432/postgres
spring.datasource.username=postgres.rewjrztuehjsgvxflnmc
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# 2. HIBERNATE / JPA SETTINGS
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.datasource.hikari.connection-timeout=30000

# 3. AI CONFIGURATION (OpenAI Mask for Groq)
spring.ai.openai.api-key=${SPRING_AI_GROQ_API_KEY}
spring.ai.openai.base-url=https://api.groq.com/openai
spring.ai.openai.chat.options.model=llama3-8b-8192

# 4. SERVER SETTINGS
server.port=8080